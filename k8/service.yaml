apiVersion: v1
kind: Service
metadata:
  name: rag-web-svc
  namespace: prod
spec:
  type: LoadBalancer           # easiest way to get a public IP now
  selector: { app: rag-web }
  ports:
  - name: http
    port: 80
    targetPort: 5000
